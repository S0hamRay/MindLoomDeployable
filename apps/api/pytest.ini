[pytest]
# Run async test functions without needing an explicit marker on each one.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Make the Loom API modules (documents, blob_storage, models, ...) importable
# from tests/ without installing the package.
pythonpath = .
testpaths = tests
